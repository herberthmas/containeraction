name “my first container action”
description ‘A cloud guru’
author: ‘Herberth Mas’
inputs:
 	myInput:
   		description: ‘greting to use’
		required: true
		default: ‘Awesome Guru’
outputs:
	myOutput:
		description: ’total memory’
runs:
	using: ‘docker’
	image: ‘Dockerfile’
